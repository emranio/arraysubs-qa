# Execution calendar — D0 to D10

> **D0 = 2026-08-02.** Execution runs D0..D10 (2026-08-02 .. 2026-08-12).
> The automated watch runs D1..D12 (2026-08-03 .. 2026-08-14) — see `watch-schedule.md`.

This calendar is the rebalanced, conflict-resolved schedule. It supersedes the `day` field that
each task was originally authored with: the first draft stacked 34 tasks onto D0, which is not
executable in one sitting and would have collided on shared settings, carts, and accounts.

**Before running any day, read `plan-audit.md`.** It lists 44 concrete ways these tasks can corrupt
each other on a shared site, with the fix for each. The most important standing rules:

- Never run `wp action-scheduler run` without `--hooks=`, and prefer draining a single subscription.
- Give every browser task its own `agent-browser --session <TASK-KEY>` name — sessions share a cart.
- Global settings changed outside the D0 baseline must be restored inside the same task.
- `SLT-SETUP-99` must NOT delete anything the watch still needs; it is split into a D10 settings
  restore and a post-D12 artifact teardown.

## Corrected execution calendar — D0 … D10 (D0 = **2026-08-02**, +1 shift per audit C19)

**Standing rules (unchanged from the audit, restated with the new anchor):**
- All SLT purchases on **D0, D1, D2 execute after 12:00 site time** (C02), so anniversary renewals land in the afternoon and can never collide with the D3 09:00–11:00 sync bracket.
- **Creations and destructive meta audits before 12:00; purchases after 12:00**, every day. Intra-day ordering in the "Task keys" column is binding, not advisory.
- One agent-browser session per task, named `<role>-<TASK-KEY>` — **including admin sessions**. `close --all` only by the day's last task (named below).
- Never run two tasks concurrently under the same `slt-*` login (persistent cart). Assert cart EMPTY first and last; a non-empty cart is a STOP.
- Action Scheduler: bare drains banned every day; single-action-by-id permitted any day after a Pending-queue pre-flight; **date-meta time travel D8 only**.
- Only `SLT-SETUP-02` (D0), `SLT-SYN-04` (D3 bracket), `SLT-EML-11/12/13`, `SLT-LIFE-03`, `SLT-SW-08`, `SLT-MYA-05` and `SLT-SETUP-99A` (D10) write global settings — each inside a declared, UTC-timestamped bracket posted to `slt-catalog-registry`.

| Day | Date | Task keys — in execution order | Overnight events expected from that day's work | Load |
|---|---|---|---|---|
| **D0** | 2026-08-02 Sun | **AM:** `SLT-SETUP-01` → `SLT-SETUP-02` → `SLT-SETUP-03` → `SLT-PROD-01`, `SLT-PROD-06`, `SLT-PROD-07`, `SLT-PROD-13` → **`SLT-SYN-01A`** (week-flex audit + restore, MUST precede any flex purchase). **PM (>12:00):** `SLT-CHK-01`⊕`SLT-REN-01` (13:00–13:30, sole purchase of the control spine) → `SLT-CHK-02` (same day, before any renewal) → `SLT-CHK-14` → `SLT-LIFE-04` → `SLT-SYN-05` (**hard-pinned: 08-02 is the last day week-flex is in segment 1**). Last task closes all sessions. | Nothing renews (earliest due 08-03 PM). Both legs must be queued for every new sub: invoice `due+k−6h`, charge `due+k`. `SLT Lifetime One Time` must queue **nothing**. | 13 |
| **D1** | 2026-08-03 Mon | **AM:** `SLT-SETUP-04` (coupon expiry **2026-08-14**, C13), `SLT-PROD-05`, `SLT-PROD-12`, `SLT-PROD-14`, `SLT-PROD-16` → **`SLT-SYN-01B`** (month + daily flex audit, before their purchases) → `SLT-SYN-03` (SLT Sync Global Daily only; Excl Probe dropped per C16) → `SLT-SETUP-05` (deps now include PROD-14, C03; runs 2 days ahead of the sync bracket). **PM:** `SLT-REN-03` (12:30–13:00) → `SLT-SYN-08` (**moved from d0**) → `SLT-CHK-03` → `SLT-LIFE-05` → `SLT-EML-06` → `SLT-CPN-01`, `SLT-CPN-02` (both 18:00–19:00). | SLT Daily Core renewal #1 + CHK-02 clone renewal #1 fire 08-03 PM. Flex subs bought today schedule to **midnight boundaries**, not anniversaries. | 15 |
| **D2** | 2026-08-04 Tue | **AM:** `SLT-PROD-02`, `SLT-PROD-03`, `SLT-PROD-09`, `SLT-PROD-15` → **`SLT-SYN-02`** (variation audit, strictly before the variation purchases) → **`SLT-MYA-05`** (member rules + features meta — **must complete before 13:00**). **PM:** `SLT-REN-02` (09:00–10:00 read + evening read) → **`SLT-DUN-01` (13:00–14:00, moved from d0)** → `SLT-CHK-04`⊕`SLT-REN-04` → `SLT-CHK-05`⊕`SLT-REN-05` → `SLT-CHK-15`⊕`SLT-EML-09` → `SLT-SYN-06` → `SLT-SYN-13` (decoy bracket ≤90 min, closed same day) → `SLT-CPN-03` (18:00–19:00) → `SLT-IMP-01` (23:40–23:55). | Daily-cohort renewals 08-04 PM. `SLT Fixed Three Cycles` renewal #1 ($7.00) 08-04 PM. `SLT-REN-03`'s `_auto_renew=off` sub creates a **pending** renewal order + the only `renewal_invoice` mail of the day. | 15 |
| **D3** | 2026-08-05 Wed | **09:00–11:00 `SLT-SYN-04` EXCLUSIVE BRACKET** — sole writer of `sync_to_billing_cycle`; no cart, checkout, product save or AS action by anyone. **After 11:00:** `SLT-PROD-04`, `SLT-PROD-10`, `SLT-PROD-11`. **PM (>12:00):** `SLT-ADM-07` step 0 (slt-core buys **SLT Signup Fee Daily** — was unowned) → `SLT-CHK-09` → `SLT-SYN-14` → `SLT-ADM-05` → `SLT-EML-11` (on-hold-email OFF bracket) → `SLT-EML-01`, `SLT-EML-03`, `SLT-EML-15`, `SLT-ADM-06` (reads) → `SLT-EML-07` (late PM) → `SLT-CPN-04` (18:00–19:00) → **`SLT-EML-12` bracket 21:00–21:40** (after every D3 checkout has closed). | `SLT Retry Daily` first renewal **FAILS** 08-05 13:00–20:00 → retry #1 queued +24h. `SLT-CHK-05`'s renewal returns `requires_action`. Paddle Daily renewal #1. `SLT Renewal Price Step` renewal #2 → **$5→$20 crossover**. Week-flex + Sync Global reminders fire 08-05 00:00–06:00. | 15 |
| **D4** | 2026-08-06 Thu | **08:00–08:20 `SLT-EML-13` EXCLUSIVE BRACKET** (admin emails OFF) — **no checkout before 08:30**. **AM:** `SLT-PROD-08`, `SLT-EML-02`, `SLT-ADM-03`, `SLT-MYA-01`, `SLT-DUN-03` (hold sweep watch). **PM (>12:00):** `SLT-SW-00` (new: slt-switch buys **Plan Basic + Plan Pro** — was unowned) → `SLT-CHK-08` → `SLT-CHK-13` → `SLT-SYN-07` → `SLT-SYN-11` → `SLT-SW-09` → `SLT-ADM-07` (invoice + pay-link) → `SLT-IMP-03` (13:00+, one 10-min window). | `SLT Retry Daily` retry #1 (08-06 PM) **and** its active→on-hold transition (~14:00). `SLT Free Signup Daily` trial converts → first $8.00 charge. `SLT Fixed Three Cycles` renewal #2 + **EXPIRY**. `SLT-EML-07`'s scheduled cancel fires. `SLT-ADM-05`'s admin-created sub renews to a **pending** order. Flex Daily Two Seg renewal #1 + Next Cycle reminder at 00:00–06:00. | 14 |
| **D5** | 2026-08-07 Fri | **AM:** `SLT-MYA-02` (08:00–11:00, before any slt-core anniversary), `SLT-DUN-02` (retry-ladder reads), `SLT-EML-04` (mail-content rider on the ladder), `SLT-ADM-09`, `SLT-IMP-02`. **PM (>12:00):** `SLT-CHK-06` (no order) → `SLT-CHK-10` → `SLT-CHK-11` → `SLT-CHK-12` → `SLT-SW-01` → `SLT-SW-06` → `SLT-SYN-12` → **`SLT-LIFE-03`** (skip-settings bracket ≤30 min, restored in-task; dates corrected to D_now=08-08 → skip1 08-09). | Retry #2 08-07 PM. Variable-flex Full renewal #1 at 00:00–06:00. Day/1 cohort renewals 08-07 PM. `SLT-CPN-03` sub A hits its **$10.00 drop-off** 08-07 evening. | 13 |
| **D6** | 2026-08-08 Sat | **AM (<12:00):** `SLT-ADM-04` (status ladder on SUB-B — body requires before noon), `SLT-EML-05`, `SLT-MYA-03` (08:00–11:00). **PM (>12:00):** `SLT-SYN-10` step 0 (slt-flex3 buys **Flex Month segment-3** — was unowned; must be 08-08) → `SLT-CHK-07` → `SLT-SW-03` → `SLT-SW-05` → `SLT-SW-07` → `SLT-SW-10` → `SLT-LIFE-02` (early renew, target **re-pointed to SLT-CHK-02's subscription**) → `SLT-IMP-04`. | **The only genuine unattended flex renewal in the window:** week seg-1 + seg-2 both charge $14.00 at 08-08 00:00 site (+k). Sync Global Daily renewal #1 and `SLT Flex Qty Week` $29.97 at the same boundary. Retry #3 / 4th charge hits the **3-retry cap** 08-08 PM. `SLT Trial Four Day` converts. Box Daily renewal #1. | 12 |
| **D7** | 2026-08-09 Sun | **AM:** `SLT-SYN-09` (reads the 08-08 and 08-09 midnight renewals), `SLT-ADM-01`, `SLT-MYA-04`. **PM:** `SLT-SW-04` (uses **slt-switch4**, not slt-switch2) and its proration order **paid** → **then** `SLT-SW-08` (switch-fee bracket, opened only after SW-04 closes) → `SLT-DUN-04` (~14:00–16:00, terminal cancellation) → `SLT-DUN-05` (after 16:00, buys S2 on the 0341 card). | `SLT Retry Daily` **CANCELLED** ~14:00–16:00 — the full active→failed→3 retries→on-hold→cancelled ladder closes. Flex Daily Two Seg #2 and Next Cycle #1 at 08-09 00:00–06:00. `SLT-LIFE-03`'s shifted $20.00 cycle charges 08-09 PM. | 8 |
| **D8** | 2026-08-10 Mon | **SOLE AUTHORIZED TIME-TRAVEL DAY — strict order, one shared pre-flight:** (0) **`SLT-TT-00`** (new): Pending-queue screenshot + the 13 non-SLT `_next_payment_date` snapshot, published to the registry and quoted by every other D8 task; then it executes month seg-1/seg-2, week seg-3 and the flex-variable tail renewals, single-action-by-id only. (1) `SLT-SYN-10`. (2) `SLT-SW-02` (Leg A downgrade; **Leg B owns** the hand-set `_end_date` expiry + auto-downgrade). (3) `SLT-EML-08` (observation-only on SW-02 Leg B; reactivates S_EML). (4) `SLT-EML-10` (expiring-soon + card-expiring probes; cancels S_EML at teardown). (5) `SLT-LIFE-01` (late-renewal phases A/B — **last**, it deliberately leaves S5 legless). (6) `SLT-EML-14` (read-only sweep). Close with the shared post-drain non-SLT diff. | `SLT-DUN-05`'s S2 renewal **FAILS** 08-10 PM. Variable-flex Full #2 and Next Cycle #1 at 08-10 00:00–06:00. Box Daily #2. Day/1 cohort renewals 08-10 PM. **No non-SLT `_next_payment_date` may move.** | 7 |
| **D9** | 2026-08-11 Tue | **AM:** `SLT-DUN-05` recovery leg (new card + pay the failed order **before** `N+24h`, i.e. before ~16:30). Then `SLT-ADM-08` (refund + cancels S_FEE a day early — declare in the registry), `SLT-ADM-02` (read-only, wants a sub with several renewals), `SLT-ADM-10` (read-only capability matrix), `SLT-IMP-05` (end-of-window log sweep + reconciliation — must run **before** SETUP-99A). | Final natural renewals of the day/1 cohort 08-11 PM. `SLT-SYN-11`'s day/5 probe renews 08-11. `SLT-SW-08`'s Enterprise sub charges **$30.00**. S2 must be **active, never on-hold**. | 5 |
| **D10** | 2026-08-12 Wed | **`SLT-SETUP-99A` only** — run **after** the morning watch read and after `SLT-IMP-05` and `SLT-DUN-05` are closed. Part 1 settings restore (five booleans, empty `jq` diff) **plus cancellation of the completed-evidence cohort only**. **No deletions.** Publish both cohort lists (cancelled-on-D10 vs tail) to the registry so the watcher can assert on D11/D12. Evidence consolidation + registry export. | Flex Daily Two Seg #3 and `SLT-SYN-12`'s Stripe probe renew at 08-12 00:00–06:00; Box Daily #3 08-12. The **tail cohort stays alive**. 99A's cancellation mails are an expected teardown side effect — count the ids. | 3 |

**After D10 — not part of the execution calendar but part of the plan:**
`SLT-SETUP-99B` runs **2026-08-15 (Sat)**, strictly after the D12 watch report and `SLT-EML-14`'s 2026-08-14 delta sweep are written: cancel the tail cohort (both week flex subs, Sync Global Daily, the two `SLT-SYN-13` variation subs, the two `SLT-SYN-12` probes, `SLT-SYN-14`'s qty sub, Box Daily, both lifetime controls, the flex month subs, `SLT-SYN-11`'s probes) then Parts 2–4 deletion of every SLT product, coupon, page, user, order and subscription. Running it on 08-13 or 08-14 destroys the last renewals of the window.

**Load balance:** 13, 15, 15, 15, 14, 13, 12, 8, 7, 5, 3. Median = 13, peak = 15, **peak/median = 1.15×** (band is 1.6×). D0 drops from the draft's 25 to 13 by moving `SLT-DUN-01` → D2, `SLT-SYN-08` → D1, `SLT-EML-06` → D1, `SLT-SETUP-04`/`SLT-PROD-05`/`SLT-PROD-16` → D1, and by merging the four duplicate-purchase pairs. D9/D10 are deliberately light — they are consolidation and teardown days that must not carry new purchases.