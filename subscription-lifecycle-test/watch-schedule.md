# Automated watch schedule — D1 to D12

> **D0 = 2026-08-02.** Watch days D1 (2026-08-03) through D12 (2026-08-14).

`automation/daily-renewal-check.sh` runs at 02:10 UTC (08:10 site-local) each day and hands the row
for that day to a non-interactive Codex session. **That agent reads this table and little else**,
so each row must be self-sufficient: what should have renewed overnight, which emails should have
arrived, which retries are due, which grace transitions and expirations should have happened, what
must show *no* activity, and which test tasks to execute that day.

## Standing checks — run these every single day, in addition to the row

1. **Firing windows, not points.** Every scheduled leg is shifted by the subscription's spread offset
   `crc32('arraysubs-spread-'.$id) % 21600` (0-6 h). Charge fires at `due + offset`, invoice at
   `due + offset - 6h`. A renewal that has not fired exactly at `due` is *not* late.
2. **Failed actions.** Any `arraysubs%` action in `failed` status in the last 36 h is investigated and
   filed, always — no exceptions, even if unrelated to today's row.
3. **Unexpected mail.** Reconcile every Mailpit message since the last watch against the expected set.
   An unexpected email is a bug just as much as a missing one.
4. **No silent forcing.** If something that should have fired did not, capture the evidence *first*
   (Action Scheduler row, subscription meta, logs), file the issue, and only then consider forcing it
   to unblock the rest of the day. Record that you forced it.
5. **Duplicate gateway meta.** For each subscription created since the last watch, compare row count to
   distinct key count for `_payment_gateway` / `_gateway_status` / `_gateway_customer_id` /
   `_payment_method_last4` (see `issues/SLT-OBS-01-*`). A fresh duplicate upgrades that issue to high.
6. **Board hygiene.** Move every task you touch, and leave `blocked` tasks with a written reason.

## Automated watch — D1 … D12 (2026-08-03 … 2026-08-14). Runs 02:10 UTC = **08:10 site (UTC+6)**.

### Read this first — it applies to every row

**Firing windows.** Nothing fires at `_next_payment_date`. For every subscription compute
`k = crc32('arraysubs-spread-' . <SUB_ID>) % 21600` (0–21600 s = 0–6 h), stable forever per id.
- **Invoice leg** `arraysubs_generate_renewal_invoice` at `due + k − 6h`; **charge leg** `arraysubs_process_renewal` at `due + k`. Assert the *window* `[due+k, due+k+5min]`, never a point.
- **ANNIV(hh:mm)** = an anniversary subscription (global sync OFF): due at its checkout clock time, so the observable window is `[hh:mm, hh:mm+6h]` **on the previous afternoon/evening**. Readable at the next morning's 08:10 run.
- **MIDNIGHT** = a flex-synced or globally-synced subscription: due at 00:00 site, window `[00:00, 06:00]` site **on the reporting day itself**. 08:10 is the earliest safe read.
- **Retries are NOT spread**: `time() + 86400` exactly, max 3, so attempts land at `D+k`, `+24h`, `+48h`, `+72h`.
- **Grace**: active→on-hold at the first hourly `arraysubs_check_overdue_renewals` tick after `D + 24h`; on-hold→cancelled at the first tick after `max(D + 96h, _on_hold_date + 72h)`.

**Unconditional checks, every single day D1–D12 — a failure on any of these is critical:**
1. `SLT Lifetime One Time` / `slt-core` (`SLT-CHK-14`) — `_next_payment_date` EMPTY, `_end_date` absent, **zero** rows in `wp_actionscheduler_actions` for its id in any status or group, **zero** renewal orders, **zero** mail naming it. From D3 also assert the same for `SLT Lifetime One Time` / `slt-email` (`H1`, `SLT-EML-11`), and from D4 for `SLT Excl Lifetime Probe` / `slt-excl` (`SLT-SYN-11` probe C).
2. No **non-SLT** subscription's `_next_payment_date` moved. Diff the 13 pre-existing active subscriptions against the previous day's snapshot.
3. `mailpit-agent list 50` — every message maps to a row below or to a registry-declared task exception. **Unmapped mail is a finding.** Snapshot `latest-id` at the start of each run.
4. Zero failed actions referencing an SLT subscription id.

   > **`wp action-scheduler list` does not exist on this site** — the installed Action Scheduler WP-CLI
   > exposes only `run`, `status`, `source`, `clean` and friends. Any task or note telling you to run
   > `action-scheduler list` is wrong; use the SQL below or the **Tools → Scheduled Actions** admin screen.

   ```bash
   wp db query "SELECT a.action_id, a.hook, a.status, a.args, a.scheduled_date_gmt, a.last_attempt_gmt
                FROM wp_actionscheduler_actions a
                WHERE a.hook LIKE 'arraysubs%' AND a.status='failed'
                  AND a.last_attempt_gmt >= DATE_SUB(UTC_TIMESTAMP(), INTERVAL 36 HOUR)
                ORDER BY a.last_attempt_gmt DESC;" --allow-root
   ```

   The daily facts snapshot in `automation/logs/D<NN>-<date>-facts.txt` already runs this query plus the
   pending and recently-attempted variants — read it before issuing your own.
5. **No forcing.** A renewal that did not fire is a genuine bug: capture the pending row, the subscription notes and the `_next_payment_date` first, file the issue, then move on. Never `wp action-scheduler run`.

**Registry exceptions the watcher must load before classifying mail** (each is posted by the owning task before it acts): `SLT-EML-02` D4 and `SLT-EML-05` D6 set `_auto_renew=off` on `SUB_CORE` for one cycle — one `Invoice for subscription #SUB_CORE` and one customer-paid renewal order are **expected** on those days; `SLT-ADM-07` D4 and `SLT-MYA-04` D7 customer-pay an `S_FEE` invoice inside the 6-hour window — those cycles are **not** unattended charges; `SLT-LIFE-02` D6 pays `SLT-CHK-02`'s cycle early — that subscription shows **no** unattended renewal on 08-09; `SLT-LIFE-03` D5 skips `S5`'s 08-08 cycle; `SLT-EML-12` (D3 21:00–21:40) and `SLT-EML-13` (D4 08:00–08:20) and `SLT-SW-08` (D7) are global brackets with recorded UTC bounds.

---

| Watch day / date | Should have renewed overnight (+ window) | Emails that must have arrived | Retry attempts due | Grace / status transitions due | Expirations & conversions | MUST show NO activity (negatives) | Tasks the watcher executes that day |
|---|---|---|---|---|---|---|---|
| **D1 · 2026-08-03 Mon** | **Nothing.** All D0 purchases were after 12:00 on 08-02, so the earliest due is 08-03 PM — after this run. Assert instead that both legs are **queued** for every D0 subscription at `due+k−6h` and `due+k`. | `new_subscription` + `admin_new_subscription` + WC New order + WC Processing order for the five D0 purchases: `SLT-CHK-01` (S1/SUB_CORE, slt-core), `SLT-CHK-02` (slt-core2 — plus **no** "Your account on" mail, notification unticked), `SLT-CHK-14` (lifetime, slt-core), `SLT-LIFE-04` (Fixed Three Cycles, slt-core), `SLT-SYN-05` (week seg-1, slt-flex). | none | none | none | Lifetime control. **Zero renewal orders site-wide.** No `renewal_invoice`, no `Payment received`, no `renews soon` (3-day lead > day/1 and day/2 cycles), no `free trial`. Fixed Three Cycles not due until 08-04. Week seg-1 not due until 08-08. | `SLT-SETUP-04`, `SLT-SETUP-05`, `SLT-PROD-05`, `SLT-PROD-12`, `SLT-PROD-14`, `SLT-PROD-16`, `SLT-SYN-01B`, `SLT-SYN-03` (AM); `SLT-REN-03` (12:30–13:00), `SLT-SYN-08`, `SLT-CHK-03`, `SLT-LIFE-05`, `SLT-EML-06`, `SLT-CPN-01`, `SLT-CPN-02` (18:00–19:00) |
| **D2 · 2026-08-04 Tue** | ANNIV(13:00) **S1 / SUB_CORE** renewal #1 **$10.00** — the control spine, `_completed_payments` 1→2, `_next_payment_date` +24h *from `_renewal_scheduled_date`* (zero `k`-sized drift). ANNIV(PM) **slt-core2** renewal #1 $10.00. Renewal orders must carry `_is_renewal_order=yes`, `_renewal_cycle_number=1`, **`created_via` EMPTY** (a `store-api` value means a human placed it). | `payment_successful` ×2 + WC **New order** (admin) ×2. **Zero** customer WC processing/completed mail for renewal orders (blanked recipient). Plus signup sets for the seven D1 purchases: CHK-03 (incl. WC **Customer new account** for slt-guest-d0), REN-03, LIFE-05, CPN-01, CPN-02, EML-06, SYN-08 ×2. | none | none | none | Lifetime. Fixed Three Cycles (due 08-04 PM). Flex Daily Two Seg (due 08-06 MIDNIGHT). Flex Daily Next Cycle (due 08-09). Week seg-1 (08-08). No `renewal_invoice`, no `renews soon`. AS logs must read **`via WP Cron`**, never `via WP CLI`. | `SLT-PROD-02/03/09/15`, `SLT-SYN-02`, `SLT-MYA-05` (all before 12:00); `SLT-REN-02`, **`SLT-DUN-01` (13:00–14:00)**, `SLT-CHK-04`⊕`REN-04`, `SLT-CHK-05`⊕`REN-05`, `SLT-CHK-15`⊕`EML-09`, `SLT-SYN-06`, `SLT-SYN-13`, `SLT-CPN-03` (18:00–19:00), `SLT-IMP-01` (23:40) |
| **D3 · 2026-08-05 Wed** | ANNIV: S1 #2 $10.00; slt-core2 #2; **Fixed Three Cycles renewal #1 $7.00** (day/2, `_completed_payments`→2, next due 08-06); **S5 Renewal Price Step renewal #1 $5.00** (`_renewal_cycle_number=2`, still the low price); slt-guest-d0 #1 $10.00; **slt-cpnrec #1 $8.00 with fee `Recurring Discount: sltpct20rec` −2.00**; **slt-cpnfirst #1 $10.00 full price, zero fees, zero coupons**; S_EML #1 $10.00. **`SLT-REN-03`'s `slt-invoice` sub:** a **`wc-pending`** renewal order created at `due+k−6h`, and the charge leg completed WITHOUT charging, leaving note `Renewal order created. Awaiting manual payment.` MIDNIGHT 00:00–06:00: **`renewal_upcoming` reminders** for week seg-1 (due 08-08), Sync Global Daily and any week-cycle sub. | `payment_successful` ×7. **`renewal_invoice` — exactly one, for `slt-invoice` only** (`_auto_renew=off`). `renews soon` for the week/day-3 cohort. `trial_started` ×2 for `slt-trial` (Free Signup Daily, Trial Four Day). Signup sets for the D2 purchases: DUN-01, CHK-04 (webhook-paid, delayed), CHK-05, CPN-03 ×2, SYN-06 ×2, SYN-13 ×2, IMP-01. | none — `S_FAIL`'s D is 08-05 13:00–14:00, i.e. after this run | none | none | Lifetime, H1. `SLT Retry Daily` not due until this afternoon. Paddle Daily renewal #1 not due until this afternoon. Month flex (`2026-09-01`). Week seg-1/seg-2 (08-08). **No `Payment received` for `slt-invoice`.** No `renews soon` for any day/1 or day/2 sub. No `expiring_soon` anywhere. | **`SLT-SYN-04` 09:00–11:00 EXCLUSIVE**; `SLT-PROD-04/10/11`; then `SLT-ADM-07` step 0 (S_FEE purchase), `SLT-CHK-09`, `SLT-SYN-14`, `SLT-ADM-05`, `SLT-EML-11`, `SLT-EML-01`, `SLT-EML-03`, `SLT-EML-15`, `SLT-ADM-06`, `SLT-EML-07`, `SLT-CPN-04` (18:00–19:00), **`SLT-EML-12` 21:00–21:40 EXCLUSIVE** |
| **D4 · 2026-08-06 Thu** | **MIDNIGHT 00:00–06:00:** Flex Daily Two Seg (`SUB_A`, slt-flex2) renewal #1 **$9.00** — first flex-synced renewal of the window; **`renewal_reminder` for `SUB_NC`** (Flex Daily Next Cycle, due 08-09 − 3d + k) — `SLT-EML-01`'s primary assertion. **ANNIV:** S1 #3; slt-core2 #3; **S5 renewal #2 → $20.00 — the `_renewal_price_after=2` crossover, one cycle earlier than PROD-05 predicted**; slt-guest-d0; cpnrec $8.00; cpnfirst $10.00; **cpncyc #1 $7.50 and cpncyc2 #1 $7.50**; **Paddle Daily renewal #1 $11.00** (Paddle-driven — the local charge leg must complete having charged nothing, leaving the note *awaiting automatic charge from Paddle*; money arrives on `transaction.completed`); **`SLT-CHK-05`'s renewal returns `requires_action`** — order stays **`wc-pending`, never `wc-failed`**, `_payment_retry_attempts` = 0; `S_TZ` (`SLT-IMP-01`, due 08-05 23:45) charges across local midnight — **the due date must still render 2026-08-05 23:45 site everywhere**; `SLT-ADM-05`'s admin-created sub produces a **`pending`** renewal order (manual fallback). **`SLT Retry Daily` renewal FAILS** at `D+k` (08-05 13:00–20:00): order `failed`, subscription **still `arraysubs-active`**, `_payment_retry_attempts=1`, `_payment_retry_next_attempt_at` = attempt + **exactly 86400 s**, note `retry_scheduled`. | `payment_successful` for each successful renewal. **`payment_failed` ×2 — one customer (`slt-fail@example.test`), one admin — identical subjects, distinguished by `To:`.** `renewal_requires_verification` to `slt-sca` (subject `Verify your subscription renewal #…`). `renewal_invoice` for `slt-invoice` and for `SLT-ADM-05`'s sub. `renews soon` for `SUB_NC`. **Registry exception:** `SLT-EML-02` produces one `Invoice for subscription #SUB_CORE` + one `Payment received` today. | **Attempt 0 (the initial charge) failed 08-05 PM.** Retry #1 is queued for 08-06 13:00–20:00 — **after** this run. | **`slt-invoice` → `arraysubs-on-hold`** (D=08-04, first sweep after D+24h = 08-05 PM), `_on_hold_date` set, one **customer-only** `subscription_on_hold` mail — **no admin counterpart exists**. **`SLT Retry Daily` MUST still be `arraysubs-active`** — hold is not due until 08-06 PM. | none | Lifetime, H1. **No `payment_failed` for `slt-sca`** — `requires_action` is not a failure. **No `trial_ending` / `renews soon` for `SLT Free Signup Daily`** (2-day trial vs 3-day lead — that suppression is the product's purpose). No `expiring_soon` anywhere. Month flex. Week seg-1/seg-2. `SUB_NC` (08-09). **Paired check:** a reminder ACTION may exist for `SLT Trial Four Day`; a reminder MAIL must not (handler requires status exactly `arraysubs-active`, the sub is `arraysubs-trial`). | **`SLT-EML-13` 08:00–08:20 EXCLUSIVE (no checkout before 08:30)**; `SLT-PROD-08`, `SLT-EML-02`, `SLT-ADM-03`, `SLT-MYA-01`, `SLT-DUN-03`; then `SLT-SW-00`, `SLT-CHK-08`, `SLT-CHK-13`, `SLT-SYN-07`, `SLT-SYN-11`, `SLT-SW-09`, `SLT-ADM-07`, `SLT-IMP-03` |
| **D5 · 2026-08-07 Fri** | **MIDNIGHT:** `SLT Flex Variable Daily` **Full** variation renewal #1 **$12.00** (`SLT-SYN-13`). **ANNIV:** S1 #4; slt-core2 #4; S5 #3 **$20.00**; slt-guest-d0; cpnrec $8.00; **cpncyc #2 $7.50 → `_coupon_remaining_cycles` 0 + exhaustion note**; cpncyc2 #2 $7.50; cpnfirst $10.00; Paddle #2; slt-sca (verified path, `_completed_payments`=2, next date from `_renewal_scheduled_date`); **`SLT Free Signup Daily` trial converted 08-06 → first real charge $8.00**; **`slt-chk-qty` renewal #1 $30.00 with line quantity 3** (proves `_recurring_amount × _quantity`); **`slt-cpnrej` renewal #1 $10.00 — one line item, ZERO fees, ZERO coupons**; `S_FEE` renewal #1 $9.00 (**no signup-fee line**; paid early inside the invoice window by `SLT-ADM-07` — registry exception); **`SLT Fixed Three Cycles` renewal #2 $7.00 → `_completed_payments`=3 ≥ length → `_end_date` stamped at the charge moment and status flips to `arraysubs-expired` in the same payment**; `S_EML` cancelled 08-06 PM by `SLT-EML-07`'s scheduled action. **`SLT Retry Daily` retry #1** fired 08-06 13:00–20:00 on the **same** failed order: attempts 1→2, next attempt +86400 s. | `payment_successful` per success. **`payment_failed` pair #2.** **`subscription_on_hold` to `slt-fail@example.test` — customer only, no admin copy.** **`subscription_expired` ("has expired") to `slt-core` for Fixed Three Cycles.** `subscription_cancelled` + `admin_subscription_cancelled` for `S_EML`. `trial_converted` for Free Signup Daily. `renews soon` for the day/3 flex cohort due 08-10. | **Retry #1 done (08-06). Retry #2 due 08-07 13:00–20:00** — after this run. | **`SLT Retry Daily` → `arraysubs-on-hold`** at the first hourly sweep after `D+24h` = 08-06 ~14:00–15:00. Retries must keep firing after the hold (`process()` accepts `arraysubs-on-hold`) — cancel not due until 08-09. `slt-invoice` stays on-hold (cancel ≈ 08-08). `SLT-ADM-05`'s sub enters its own hold path from its unpaid renewal order. | **Fixed Three Cycles EXPIRED 08-06.** Free Signup Daily trial → active. | Lifetime, H1, Excl Lifetime Probe. **No `expiring_soon` for Fixed Three Cycles** — the 7-day lead exceeds its life; that suppression is the point. No third renewal order for Fixed Three Cycles and **zero pending legs** for it. No `subscription_cancelled` for `slt-fail` (not due until 08-09). Month flex. Week seg-1/seg-2 (08-08). `SUB_A` next 08-09. | `SLT-MYA-02` (08:00–11:00), `SLT-DUN-02`, `SLT-EML-04`, `SLT-ADM-09`, `SLT-IMP-02`; then `SLT-CHK-06`, `SLT-CHK-10`, `SLT-CHK-11`, `SLT-CHK-12`, `SLT-SW-01`, `SLT-SW-06`, `SLT-SYN-12`, **`SLT-LIFE-03`** (≤30 min settings bracket) |
| **D6 · 2026-08-08 Sat** | **MIDNIGHT 00:00–06:00 — the headline day for flex:** **week seg-1 (`SUB_W1`, slt-flex) renewal #1 $14.00** and **week seg-2 (`SUB_W`, slt-flex2) renewal #1 $14.00 — FULL, not the $6.00 prorated first charge** (`_renewal_sync_initial_recurring_amount` must never be reused); both anchored `_renewal_scheduled_date = 2026-08-07 18:00:00 UTC`, both advancing to 2026-08-14 18:00:00. **`SLT Flex Qty Week` renewal $29.97** (3 × $9.99, proving proration hit only the first charge). **Sync Global Daily renewal #1** on the midnight boundary even though the global switch is back OFF — `_renewal_sync_enabled=yes` on the subscription is what governs. **ANNIV:** S1 #5; slt-core2 #5; slt-guest-d0; cpnrec $8.00; cpnfirst $10.00; **cpncyc #3 → $10.00 FULL PRICE, zero fee items — the `count_initial=yes` drop-off**; cpncyc2 #3 $7.50; Paddle #3; slt-sca; Free Signup $8.00; `slt-chk-qty` $30.00; `slt-cpnrej` $10.00; `S_FEE` $9.00; **`SLT-CHK-08` SUB1 $10.00 + SUB2 $9.00 (no fee line on the renewal)**; **`SLT-IMP-03` ×3, all $10.00, at three DIFFERENT timestamps** despite dues within 10 minutes — the spread-offset stagger proof, zero rows under `status=failed`; **`SLT-SW-09` slt-retain #1 $4.00 with a −$1.00 `Retention Discount` line**, slt-retain2 #1 **$15.00**; **`SLT-SW-01`'s upgraded sub charges $15.00 on the UNCHANGED due date**; `SLT-SW-06`'s sub **$15.00**; `SLT-CHK-11` Starter $6.00; `SLT-CHK-12` $10.00; **Box Daily renewal #1 $10.00 with the same contents and no wizard re-run**; `S_TZ`. **`SLT Retry Daily` retry #2** fired 08-07: attempts 2→3. | `payment_successful` per success (expect ~20). **`payment_failed` pair #3.** `renews soon` for `SLT-SYN-11`'s day/5 probe (due 08-11). **Registry exception:** `SLT-EML-05` produces one multipart `Invoice for subscription #SUB_CORE` + one `Payment received`. | **Retry #2 done (08-07). Retry #3 — the 4th and final charge — due 08-08 13:00–20:00**, after this run. | `SLT Retry Daily` remains `arraysubs-on-hold`; cancel due 08-09. **`slt-invoice` → `arraysubs-cancelled`** at ≈ 08-08 (`max(D+96h, hold+72h)`) — check late in the day or on D7. | `SLT Trial Four Day` trial ends 08-08 PM; bulk conversion runs 02:00 UTC so the converted state and `trial_converted` mail land on **D7**. `SLT-SYN-11` probe B trial converts 08-08. | Lifetime, H1, Excl Lifetime Probe. **`S5` (Renewal Price Step) must show NO renewal for the 08-08 cycle — `SLT-LIFE-03` skipped it (registry exception); it must also send ZERO mail on skip/undo/modify.** `SUB_A` and `SUB_NC` (both 08-09). `SLT-SYN-13` Next Cycle (08-10). Month flex. Week seg-3 (08-15). No `expiring_soon`. | `SLT-ADM-04` (before 12:00), `SLT-EML-05`, `SLT-MYA-03` (08:00–11:00); then `SLT-SYN-10` step 0 (month seg-3 purchase, must be 08-08), `SLT-CHK-07`, `SLT-SW-03`, `SLT-SW-05`, `SLT-SW-07`, `SLT-SW-10`, `SLT-LIFE-02`, `SLT-IMP-04` |
| **D7 · 2026-08-09 Sun** | **MIDNIGHT:** **`SUB_A` (Flex Daily Two Seg) renewal #2 $9.00** and **`SUB_NC` (Flex Daily Next Cycle) renewal #1 $9.00** — the one-cycle-push proof, dues exactly 259200 s apart, same `k` on the re-queued legs. **ANNIV:** S1 #6; S5 **shifted $20.00 charge** (skip complete, `_skip_cycles_remaining`=0, meta cleared); slt-guest-d0; cpnrec $8.00; cpnfirst $10.00; cpncyc $10.00; **cpncyc2 #4 → $10.00 FULL PRICE — the `count_initial=no` drop-off, one cycle later than sub A**; Paddle; slt-sca; Free Signup; qty $30.00; cpnrej $10.00; S_FEE $9.00; CHK-08 ×2; IMP-03 ×3; **slt-retain #2 $4.00**, slt-retain2 **#2 $5.00 (the accepted downgrade applied)**; SW-01 sub $15.00; SW-06 sub $15.00; CHK-11 Starter $6.00; **CHK-11 Plus $11.00 with NO $4.00 fee line**; CHK-12 $10.00; **`SLT-CHK-07` mixed-cart sub renews $10.00 with ONE line item — any $3.00 line is a defect**; `SLT-IMP-04`'s three orphan probes (trashed product / edited price / deleted customer) — **record the actual outcome, assert no spec**. **`SLT Retry Daily` 4th charge** fired 08-08: fails, `_payment_retry_attempts` stays **3** (not 4), note *reached retry limit*, **no further pending `arraysubs_process_renewal`**. | `payment_successful` per success. **`payment_failed` pair #4 — 8 failure messages cumulative (4 customer, 4 admin), no dedupe.** **`subscription_cancelled` + `admin_subscription_cancelled` for `slt-invoice`.** **`trial_converted` for `SLT Trial Four Day`** + its $12.00 renewal order. `renews soon` for the `SLT-SYN-12` probes (due 08-12). | **All three retries exhausted.** No 5th attempt may ever fire. | `SLT Retry Daily` still `arraysubs-on-hold` — **cancellation is due today ~14:00–16:00, i.e. after this run**, so today it is asserted as still on-hold. `slt-invoice` cancelled. | `SLT Trial Four Day` → `arraysubs-active`. `SLT-SYN-11` probe B converted 08-08 ($20.00). | Lifetime, H1, Excl Lifetime Probe. **`slt-core2` must show NO unattended renewal** — `SLT-LIFE-02` paid that cycle early on 08-08 (registry exception). Month flex. Week seg-1/seg-2 (08-14). Week seg-3 (08-15). No 5th `payment_failed`. No `Payment received` for `slt-fail`. | `SLT-SYN-09`, `SLT-ADM-01`, `SLT-MYA-04` (AM); `SLT-SW-04` **then** `SLT-SW-08` (fee bracket, opened only after SW-04's proration order is paid); `SLT-DUN-04` (~14:00–16:00); `SLT-DUN-05` (after 16:00) |
| **D8 · 2026-08-10 Mon** | **MIDNIGHT:** `SLT-SYN-13` **Full** renewal #2 $12.00 and **Next Cycle** renewal #1 $12.00 — their dates must differ by exactly one 3-day cycle (the variation-beats-parent-decoy proof). **ANNIV:** S1 #7; slt-core2 #6 (back on the grid, one cycle further on); S5 back to normal; the full day/1 cohort; **`SLT-SW-07`'s Plus sub renews on its re-anchored day/2 date**; **Box Daily renewal #2 $10.00**; `SLT-SW-04`'s upgraded sub. **`SLT Retry Daily` CANCELLED** at ≈ 08-09 14:00–16:00: `_end_date` + `_cancelled_date` set, `_cancelled_by=system`, `_cancellation_reason=overdue_payment`, the failed renewal order moved to `cancelled`, **zero pending actions in group `arraysubs_renewals` for it**. The ladder active → failed → 3 retries → on-hold → cancelled is complete: **4 charges, 8 payment-failed messages, 5 days.** `SLT-SW-10`'s sub cancelled at `CANCEL_AT` **exactly, with no spread offset** (contrast the renewal legs at `±k`), then reactivated — record whether reactivation re-queued any legs (a known bug candidate: no `RenewalScheduler::schedule()` call). | **`subscription_cancelled` + `admin_subscription_cancelled` for `slt-fail`** — different subjects, check `To:`. `subscription_cancelled` pair + `subscription_reactivated` for `SLT-SW-10`'s sub. `payment_successful` per success. | none — the ladder is closed | **`SLT Retry Daily` → `arraysubs-cancelled`.** | none | Lifetime, H1, Excl Lifetime Probe. **No `payment_failed` for `slt-fail` after the cancel time; no 5th charge; no `subscription_expired` for it (this is a cancellation, not an expiry).** Month flex — **this is the last morning before the time-travel task touches it**; snapshot it now. Week seg-1/seg-2 (08-14). | **SOLE TIME-TRAVEL DAY, strict order:** `SLT-TT-00` pre-flight + month seg1/seg2, week seg3, flex-variable tail → `SLT-SYN-10` → `SLT-SW-02` → `SLT-EML-08` → `SLT-EML-10` → `SLT-LIFE-01` → `SLT-EML-14`. Close with the shared post-drain non-SLT diff |
| **D9 · 2026-08-11 Tue** | **ANNIV:** the full day/1 cohort's penultimate natural renewals; **`SLT-SW-08`'s sub charges $30.00 on SLT Plan Enterprise with NO switch-fee line** (the $7.50 fee is one-off, not recurring); Box Daily is on day/2 (next 08-12); `SLT-SYN-11` probe A (day/5) renews 08-11. **`SLT-DUN-05`'s S2 renewal FAILED** 08-10 16:30–22:30: order `failed`, `_payment_retry_attempts`=1. Plus every renewal the D8 time-travel produced — month seg-1/seg-2/seg-3, week seg-3, the flex-variable tail — each with its own invoice/charge order and mail. | `payment_successful` per success. **`payment_failed` pair for S2.** Renewal/invoice mail for each time-travelled renewal. | **S2 retry #1 queued for 08-11 16:30** — the recovery must beat it and beat the `N+24h` hold. | **S2 must NOT be on-hold** — `SLT-DUN-05`'s recovery (new card + pay the failed order from My Account) runs this morning, restoring it to `arraysubs-active`, clearing `_payment_retry_*` and `_last_payment_failure*`, and setting `_next_payment_date` to **`N + 1 day` (anchored on `_renewal_scheduled_date`, not payment time)**. Any hand-set `_end_date` from D8 must have expired and auto-downgraded. | none | Lifetime, H1, Excl Lifetime Probe. `SLT Retry Daily`, `S_EML`, `slt-invoice` — **cancelled, must show nothing.** Fixed Three Cycles — **expired 08-06, must show nothing.** **No non-SLT renewal order may have been created by the D8 time travel** — diff the 13 pre-existing subs against `SLT-TT-00`'s pre-drain snapshot. | `SLT-DUN-05` recovery leg (before ~16:30), `SLT-ADM-08`, `SLT-ADM-02`, `SLT-ADM-10`, `SLT-IMP-05` (must complete before `SLT-SETUP-99A`) |
| **D10 · 2026-08-12 Wed** | **MIDNIGHT:** `SUB_A` (Flex Daily Two Seg) renewal #3 $9.00 — dues 08-06 / 08-09 / 08-12, exactly 259200 s apart, same `k` throughout (the grid-holds proof); `SLT-SYN-12`'s Stripe-leg probe renews at the midnight boundary (contrast its Paddle sibling on the anniversary). **ANNIV:** the day/1 cohort's **final natural renewals** (08-11 PM); `SLT-SYN-11` probe A renewed 08-11; **Box Daily renewal #3 $10.00** (08-12). `SLT-SW-09`'s `slt-retain` must have taken **exactly three** discounted $4.00 renewals and then **$5.00 with no `Retention Discount` line** — the N-cycle contract closing. | `payment_successful` per success. Later in the day, **`subscription_cancelled` (+ admin copy) for every subscription `SLT-SETUP-99A` cancels** — an expected teardown side effect: **count the ids, do not treat as failures.** | none | `SLT-SETUP-99A` cancels the **completed-evidence cohort only** — record every cancellation timestamp to the registry. **No deletions today.** | none | Lifetime, H1, Excl Lifetime Probe. All previously cancelled/expired subs. **Confirm no subscription entered on-hold unexpectedly** — nothing should have failed except the designated dunning cohort. Week seg-1/seg-2 (08-14) and `SLT-SYN-13` (08-13) must still be **alive and scheduled** — 99A must not have touched them. | `SLT-SETUP-99A` — run **after** this morning's read and after `SLT-IMP-05` and `SLT-DUN-05` are closed. Restore the five baseline booleans (empty `jq` diff) + cancel the completed cohort. Publish both cohort lists |
| **D11 · 2026-08-13 Thu** | **Tail-only day. MIDNIGHT:** `SLT-SYN-13` **Full** renewal (08-13) and **Next Cycle** renewal (08-13), $12.00 each — dues 08-07/08-10/08-13 and 08-10/08-13. Nothing else should renew: the day/1 cohort was cancelled on 08-12. | Only `payment_successful` for the two `SLT-SYN-13` renewals. Any teardown `subscription_cancelled` mail still arriving from D10 — count the ids against 99A's published list. | none | Any second dunning ladder started on D8/D9 would reach on-hold at +1 day here — record it or record that none exists. | none | **This is the proof-of-partial-teardown day.** Lifetime, H1, Excl Lifetime Probe (11th assertion). **Every subscription cancelled by `SLT-SETUP-99A` must show NO renewal, NO renewal order and NO mail after its recorded cancellation timestamp.** No day/1 charge anywhere. Week seg-1/seg-2 must still be pending for 08-14, not fired early. | **None** — watch only. Do **not** run `SLT-SETUP-99B` |
| **D12 · 2026-08-14 Fri** | **MIDNIGHT 00:00–06:00 — the last scheduled events in the whole window:** **week seg-1 (`SUB_W1`) and week seg-2 (`SUB_W`) renewal #2, $14.00 each** at 2026-08-13 18:00:00 UTC + `k`, `_renewal_scheduled_date` = that exact boundary; and **Sync Global Daily's final renewal** on its midnight boundary. Nothing on the anniversary track. | `payment_successful` ×3 (or however many of the tail actually fires) and **nothing else**. | none | none | none | Lifetime, H1, Excl Lifetime Probe — **12th and final negative assertion**. No renewal for anything cancelled on 08-12. `SLT Flex Qty Week` renews 08-15 and `SLT-SYN-07`'s week seg-3 renews 08-15 — **both outside the window; record the pending action, do not score a miss.** Month flex subs renew 09-01/10-01 — same treatment. | **Capture `SLT-EML-14`'s 08-14 delta sweep and write the final watch report FIRST.** Then remove the watch cron. **`SLT-SETUP-99B` runs 2026-08-15**, never today — its cancellation mail would contaminate today's silence proof |
