---
id: 43
title: 'Stripe SCA renewal observation: verify SLT-CHK-05 requires_action email and pay link'
status: done
priority: high
created: 2026-08-02T03:43:06.536690493+02:00
updated: 2026-08-05T21:37:49.475584965+02:00
started: 2026-08-05T20:56:55.20991201+02:00
completed: 2026-08-05T20:56:55.20991201+02:00
tags:
    - renewal
    - day-02
due: "2026-08-04"
estimate: 2h
depends_on:
    - 1
    - 10
    - 12
class: standard
---

> **SLT-REN-05** · group `renewal` · scheduled **D02** (2026-08-04)

> Read `README.md` (environment + isolation contract), `calendar.md` (this day's exact
> ordering — it is binding, not advisory) and `plan-audit.md` before starting.

## Objective
Observe the single subscription created by `SLT-CHK-05` when its unattended Stripe renewal hits `requires_action`, then prove the verification path: one `renewal_requires_verification` email, no failure/retry machinery, and a customer link that resolves and completes the charge. This task places no order and creates no account.

## Scope
- Gateway: Stripe test
- Checkout: N/A (renewal observation and order-pay page only)
- Account: existing `slt-sca@example.test`, created by `SLT-CHK-05`
- Plugins: pro-required

## Preconditions
- `SLT-CHK-05` has completed its sole D2 classic-checkout **setup leg**, remains `in-progress` while its renewal criteria are pending, and has published the exact `UID`, `SUBID5`, parent order, `k5`, `_next_payment_date`, and pending action IDs to the registry. Its D2 `PRE` is checkout-only and must never be reused for the D3 renewal. This card deliberately does not depend on task 30 reaching `done`, because the D3 evidence collected here is what closes task 30.
- If `SLT-CHK-05` publishes that no source subscription exists, re-check the exact user/product pair, cite its standalone issue/evidence, record this rider `UNVERIFIED (no source subscription)`, and move it through review to done. Do not create a replacement user/order or leave either execution card blocked.
- Require exactly one `slt-sca@example.test` user and exactly one SLT Daily Core subscription owned by it. Any second record is contamination: stop and write a standalone issue file under `issues/`.
- `requires_action` is NOT a failure in core: no failed order, retry, or dunning mail — but the grace clock runs (on-hold ≈ due+1 day), so complete verification before that sweep.
- No drains and no date mutation. The renewal must fire unattended on D3 = 2026-08-05 at the recorded `due+k5`.

## Test data
| Item | Value |
|---|---|
| Product | Existing SLT Daily Core subscription `SUBID5` from `SLT-CHK-05` |
| Account | existing `slt-sca@example.test` |
| Card | already stored by `SLT-CHK-05`; do not enter it again until the order-pay challenge asks |
| Session | `customer-SLT-REN-05` |

## Steps
1. On D2 resolve the registry hand-off and re-query it read-only. Require the user, `SUBID5`, parent order, `_gateway_payment_method_id`, `_next_payment_date`, `k5`, and both pending action IDs to match `SLT-CHK-05`. Publish the exact D3 charge row and `charge−5m` deadline to the registry and D02 watch report, leave this card `in-progress`, and set no renewal mail baseline yet.
2. On D3 no earlier than five minutes before the recorded `due+k5`, set `PRE5B=$(mailpit-agent latest-id)` in both handoff locations. After the gate passes, force nothing. `mailpit-agent wait-new "$PRE5B" 900 "Verify your subscription renewal"`; show/text the exact matching message, extract its action URL, and capture the message in the local Mailpit UI as `SLT-REN-05-01-verification-email.png` using `customer-SLT-REN-05`. Resolve the sole renewal order from the subscription's exact `_pending_renewal_order_id`, require a numeric ID plus the reverse `_subscription_id=SUBID5` and a +1 renewal-order count, then read its action-context meta, the exact AS rows, and logs; never select the order by recency.
3. Prove the renewal action log says `via WP Cron`; confirm the order is `wc-pending`, never `wc-failed`, and no retry action or payment-failed mail exists.
4. In `agent-browser --session customer-SLT-REN-05`, log in as `slt-sca`, open the extracted URL, re-snapshot, and screenshot the HTTP-200 order-pay form. Require `wc-stripe-confirmation=1` plus the correct order key in the URL.
5. Immediately before completing the 3DS challenge and submitting payment, set `PAY_PRE=$(mailpit-agent latest-id)`. Submit once, run `mailpit-agent wait-new "$PAY_PRE" 300 "Payment received for subscription #$SUBID5"`, and reconcile every message newer than `PAY_PRE`. Capture the unpopulated pay form as `SLT-REN-05-02-pay-page.png` and the safe paid result as `-03-order-paid.png`; never capture evidence containing a full card number. Re-read the exact renewal order, subscription meta, newly armed exact AS rows, and logs.
6. Add the renewal order ID, verification Mailpit ID, action URL shape (redact the order key in prose), payment-success Mailpit ID, and post-payment schedule to the registry. Empty the cart if present and close only `customer-SLT-REN-05`.
7. Self-review this evidence, move `SLT-REN-05` through Review to Done, then cite the same renewal evidence in `SLT-CHK-05` and move that parent card through Review to Done. Never leave either card in Review.

## Expected results
1. The existing `SLT-CHK-05` checkout baseline remains one paid $10.00 parent order and one active subscription with a stored `_gateway_payment_method_id`; this task creates no second user, order, or subscription before renewal time.
2. At the D3 `due+k5`, `arraysubs_process_renewal` completes (`via WP Cron`) as a non-failure: the renewal order stays `wc-pending`, **never `wc-failed`**.
3. Order meta `_arraysubs_payment_action_intent_id`, `_arraysubs_payment_action_required_at`, `_arraysubs_payment_action_url` set, mirrored on the subscription; `_payment_retry_attempts` 0/absent, no retry queued, `_completed_payments` stays 1, status stays `arraysubs-active`.
4. The action URL is the order-pay URL plus `wc-stripe-confirmation=1` and the order key; as the logged-in customer it returns 200 with a pay form, not a 404 or My Account redirect.
5. Completing the challenge charges $10.00: order `wc-processing`/`wc-completed`, `_completed_payments`=2, `_pending_renewal_order_id` cleared, `_next_payment_date` advances to D4 at the original anniversary time (from `_renewal_scheduled_date`, so the delay adds no drift), fresh legs at `k5`.

## Emails expected
| # | Email | Trigger point | Recipient | Subject contains | Verify with |
|---|---|---|---|---|---|
| 1 | renewal_requires_verification | `due+k5` | slt-sca@example.test | `Verify your subscription renewal #SUBID5` | `mailpit-agent wait-new "$PRE5B" 900` |
| 2 | payment_successful | after step 5 | slt-sca@example.test | `Payment received for subscription #<SUBID5>` | `mailpit-agent wait-new "$PAY_PRE" 300 "Payment received for subscription #$SUBID5"`, then inspect every newer message |
| 3 | NONE EXPECTED: signup duplicates, payment_failed, admin_payment_failed, subscription_on_hold, renewal_invoice | this observation leg | — | — | inspect the `PRE5B` and `PAY_PRE` deltas |

## Evidence to capture
- `SLT-REN-05-01-verification-email.png`, `-02-pay-page.png`, `-03-order-paid.png` (none with a full card number); the cited `SLT-CHK-05` signup evidence; `SUBID5`, user id, parent/renewal order ids, `k5`, `PRE5B`, `PAY_PRE`, redacted verification URL shape, intent id, AS rows/logs, and Mailpit ids.

## Pass criteria
- [ ] Exactly one existing `slt-sca` user/subscription from `SLT-CHK-05`; this task placed no order before renewal
- [ ] Renewal returned `requires_action`: order `wc-pending`, not failed; zero retries
- [ ] Three order metas and both sub metas written; sub still active
- [ ] Exactly one `renewal_requires_verification` and one post-payment success email; signup/failure/on-hold/invoice negatives absent
- [ ] The verification URL resolves to a pay form; paying charges $10.00
- [ ] `_completed_payments`=2, `_next_payment_date` = D4 original time, legs re-armed

## Isolation / teardown
- If verification is late the sub goes on-hold ≈ due+1 day, cancels ≈ due+4. Record which; an on-hold before step 5 changes result 5 (payment forces status active) but not results 1–4.
- Never reuse `slt-sca@example.test`; this task creates nothing. Empty any cart and close only `customer-SLT-REN-05`.

---

### Verified environment facts (2026-08-01/02 — do not re-derive)

- **Nothing fires at `_next_payment_date`.** Every scheduled leg is shifted by
  `crc32('arraysubs-spread-'.$subscription_id) % 21600` (0-6 h). Charge fires at `due + offset`,
  invoice at `due + offset - 6h`. The stored date never moves. **Assert a window, not a point.**
- Currency `USD`. **Taxes are OFF** (`woocommerce_calc_taxes = no`) — never assert a tax line.
- Orders use **HPOS** (`wp_wc_orders`), not `wp_posts`.
- `woocommerce_enable_guest_checkout = yes`, but ArraySubs force-requires registration for
  **subscription** carts via `woocommerce_checkout_registration_required`
  (`SubscriptionCheckout/Services/Hooks.php:103`, `CheckoutHelpersTrait.php:93-100`).
- WooCommerce **grouped** products have zero handling in either plugin — grouped tasks are
  exploratory: document behaviour, do not assert a spec.
- WP-Cron runs every minute from `/etc/cron.d/mirror-help-arrayhash-wordpress`. Scheduled actions
  fire on their own; **a renewal that does not fire is a real bug** — capture evidence and do not force a natural-watch action.
- Give this task its own browser session (`agent-browser --session <role>-<TASK-KEY>`). Sessions are
  keyed by name and **share a cart**.
- Never run a bare or `--hooks=` Action Scheduler drain. Run one known action ID at a time only when the task explicitly authorizes it and after the required queue pre-flight; natural-watch actions are never forced.
- Evidence goes under `/home/server-manager/slt-evidence/` using task-key-prefixed filenames.

[[2026-08-05]] Wed 20:56
UNVERIFIED (no source subscription) on 2026-08-05.

Live verification: wp user list for slt-sca returned no matching user, and the arraysubscription owner query for slt-sca@example.test returned no rows, so the source checkout fixture from SLT-CHK-05 does not exist in the live QA site. The authored D3/D4 calendar notes expect a requires_action renewal only if that D2 setup leg existed; automation logs for 2026-08-05 also treat missing D2 SCA fixtures as execution gaps that remain UNVERIFIED rather than product failures. Closing this rider per its precondition instead of creating a replacement user/order or leaving the card open.
