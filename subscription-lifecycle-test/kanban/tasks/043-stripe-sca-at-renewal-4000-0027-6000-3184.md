---
id: 43
title: Stripe SCA at renewal (4000 0027 6000 3184) — verification email and link that actually completes
status: todo
priority: high
created: 2026-08-02T03:43:06.536690493+02:00
updated: 2026-08-02T03:43:17.075365271+02:00
tags:
    - renewal
    - day-02
    - has-conflicts
due: "2026-08-04"
estimate: 2h
depends_on:
    - 11
    - 5
    - 61
class: standard
---

> **SLT-REN-05** · group `renewal` · scheduled **D02** (2026-08-04)

> Read `README.md` (environment + isolation contract), `calendar.md` (this day's exact
> ordering — it is binding, not advisory) and `plan-audit.md` before starting.

### ⚠ Conflict resolutions that apply to this task

**`critical` · duplicate-purchase / same-account collision** — with `SLT-CHK-05`

- *Problem:* Both tasks declare 'this task CREATES slt-sca / slt-sca@example.test (must not pre-exist)' and both buy SLT Daily Core with the 3DS card 4000 0027 6000 3184, then both assert the same off-session requires_action renewal, the same five _arraysubs_payment_action_* metas, the same renewal_requires_verification email and the same verification-link completion. CHK-05 is d2, REN-05 is d3. Whichever runs second aborts on its own 'no slt-sca user may exist' precondition, or - worse - proceeds and migrates CHK-05's subscription (auto_migrate_on_checkout=true, same account + same product), destroying the pending requires_action order that is the entire point of the test.
- *Required fix:* Merge. Keep SLT-CHK-05 on D2 as the sole purchaser (its 'no later than D2' constraint is the binding one - the renewal must fire, be verified, and still beat the on-hold sweep at due+24h). Fold REN-05's stronger assertions into it: the wc-stripe-confirmation=1 URL shape, the 200-with-pay-form check, the '_next_payment_date recomputed from _renewal_scheduled_date, not payment time' assertion, and the on-hold-if-late escape clause. Retire the SLT-REN-05 key or repoint it to 'verification leg of SLT-CHK-05, D3 morning'.

---
## Objective
Save Stripe's 3DS card `4000 0027 6000 3184`, let the unattended renewal hit `requires_action`, and prove the verification path: one `renewal_requires_verification` email, no failure/retry machinery, and a customer link that resolves and completes the charge.

## Scope
- Gateway: Stripe test
- Checkout: block
- Account: guest->new (`slt-sca@example.test`)
- Plugins: pro-required

## Preconditions
- SLT-SETUP-01/02 + SLT-PROD-01 done. This task **creates** `slt-sca@example.test` (must not pre-exist; deleted by SLT-SETUP-99B); `slt-core`/`slt-invoice` own Daily Core already.
- Buy **after 12:00 site on D3 = 2026-08-05**, once SLT-SYN-04's bracket closes.
- `requires_action` is NOT a failure in core: no `failed` order, no retry, no dunning mail — but the grace clock runs (on-hold ≈ due+1 day), so verify D5 morning.
- No drains. Page 8 stays on Block checkout. Renewal due 2026-08-06 ≈ purchase time.

## Test data
| Item | Value |
|---|---|
| Product | SLT Daily Core $10.00/day |
| Account | guest `slt-sca@example.test` |
| Card | 4000 0027 6000 3184 (3DS) |
| Sessions | `guest-SLT-REN-05`, `customer-SLT-REN-05` |

## Steps
1. `PRE5=$(mailpit-agent latest-id)`; assert the session cart is EMPTY and no `slt-sca` user exists. `agent-browser --session guest-SLT-REN-05 open "/?p=<Daily Core ID>"` → **Add to cart** → `/checkout/`.
2. Guest billing: `slt-sca@example.test`, `SLT Sca`, `1 SLT Way`, Dhaka BD 1207. Enter the 3DS card, tick **Save payment information**, **Place Order**, complete the challenge (**Complete authentication**). Screenshot it and the received page.
3. `wait-new "$PRE5" 180 "is active"`. Resolve `SUBID5`, user id, `_gateway_payment_method_id`, `_next_payment_date`; compute `k5`; record both legs. As admin set the user password to `SltQa!2026#Pass` for the pay link.
4. `PRE5B=$(mailpit-agent latest-id)`; wait past the D4 charge leg (`due+k5`, 2026-08-06, worst case +6h).
5. `wait-new "$PRE5B" 900 "Verify your subscription renewal"`; `text latest`; extract the URL. Re-read `SUBID5` meta, order status/meta, AS rows and logs; `list 50`.
6. D5 morning (2026-08-07, before the on-hold sweep): `--session customer-SLT-REN-05`, log in as `slt-sca`, open the URL, `snapshot -i`.
7. Complete the challenge, submit payment, screenshot before/after; re-read order, sub meta, AS rows; `list 50`.

## Expected results
1. Checkout succeeds after the challenge: order paid $10.00, subscription `arraysubs-active`, `_gateway_payment_method_id` stored.
2. At `due+k5` `arraysubs_process_renewal` completes (`via WP Cron`) as a non-failure: the order stays `wc-pending`, **never `wc-failed`**.
3. Order meta `_arraysubs_payment_action_intent_id`, `_arraysubs_payment_action_required_at`, `_arraysubs_payment_action_url` set, mirrored on the subscription; `_payment_retry_attempts` 0/absent, no retry queued, `_completed_payments` stays 1, status stays `arraysubs-active`.
4. The action URL is the order-pay URL plus `wc-stripe-confirmation=1` and the order key; as the logged-in customer it returns 200 with a pay form, not a 404 or My Account redirect.
5. Completing the challenge charges $10.00: order `wc-processing`/`wc-completed`, `_completed_payments`=2, `_pending_renewal_order_id` cleared, `_next_payment_date` advances to D5 at the original anniversary time (from `_renewal_scheduled_date`, so the delay adds no drift), fresh legs at `k5`.

## Emails expected
| # | Email | Trigger point | Recipient | Subject contains | Verify with |
|---|---|---|---|---|---|
| 1 | new_subscription + admin_new_subscription | Place Order | customer/admin | `is active` | `wait-new $PRE5 180` |
| 2 | renewal_requires_verification | `due+k5` | slt-sca@example.test | `Verify your subscription renewal #SUBID5` | `wait-new $PRE5B 900` |
| 3 | payment_successful | after step 7 | slt-sca@example.test | `Payment received for subscription #SUBID5` | `list 50` |
| 4 | NONE EXPECTED: payment_failed, admin_payment_failed, subscription_on_hold, renewal_invoice | `due+k5` | — | — | `list 50` |

## Evidence to capture
- `SLT-REN-05-01-3ds-checkout.png`, `-02-verification-email.png`, `-03-pay-page.png`, `-04-order-paid.png`; `SUBID5`, user id, order ids, `k5`, verification URL, intent id, AS rows, Mailpit ids.

## Pass criteria
- [ ] 3DS checkout gave a paid order and an active sub with saved card
- [ ] Renewal returned `requires_action`: order `wc-pending`, not failed; zero retries
- [ ] Three order metas and both sub metas written; sub still active
- [ ] Exactly one email (`renewal_requires_verification`); negatives absent
- [ ] The verification URL resolves to a pay form; paying charges $10.00
- [ ] `_completed_payments`=2, `_next_payment_date` = D5 original time, legs re-armed

## Isolation / teardown
- If verification is late the sub goes on-hold ≈ due+1 day, cancels ≈ due+4. Record which; an on-hold before step 7 changes result 5 (payment forces status active) but not 1–4.
- Never reuse `slt-sca@example.test`. Empty cart, close both sessions.

---

### Verified environment facts (2026-08-01/02 — do not re-derive)

- **Nothing fires at `_next_payment_date`.** Every scheduled leg is shifted by
  `crc32('arraysubs-spread-'.$subscription_id) % 21600` (0-6 h). Charge fires at `due + offset`,
  invoice at `due + offset - 6h`. The stored date never moves. **Assert a window, not a point.**
- Currency `USD`. **Taxes are OFF** (`woocommerce_calc_taxes = no`) — never assert a tax line.
- Orders use **HPOS** (`wp_wc_orders`), not `wp_posts`.
- `woocommerce_enable_guest_checkout = yes`, but ArraySubs force-requires registration for
  **subscription** carts via `woocommerce_checkout_registration_required`
  (`SubscriptionCheckout/Services/Hooks.php:103`, `CheckoutHelpersTrait.php:93-100`).
- WooCommerce **grouped** products have zero handling in either plugin — grouped tasks are
  exploratory: document behaviour, do not assert a spec.
- WP-Cron runs every minute from `/etc/cron.d/mirror-help-arrayhash-wordpress`. Scheduled actions
  fire on their own; **a renewal that does not fire is a real bug** — capture evidence before forcing.
- Give this task its own browser session (`agent-browser --session <role>-<TASK-KEY>`). Sessions are
  keyed by name and **share a cart**.
- Never run `wp action-scheduler run` without `--hooks=`; prefer a single action by ID.
- Evidence goes in `qa/subscription-lifecycle-test/evidence/<TASK-KEY>/`.
