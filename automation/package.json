{
  "name": "arraysubs-qa-automation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "agent-browser (Vercel) helpers for ArraySubs manual QA verification.",
  "scripts": {
    "test": "node run.mjs",
    "setup": "npm i -g agent-browser && agent-browser install",
    "install:browsers": "agent-browser install"
  }
}
